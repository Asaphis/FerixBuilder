import { ArrowRight, CheckCircle2, ShieldCheck, Sparkles } from "lucide-react";
import { Link } from "wouter";
import { startAuthentication } from "@/lib/auth";

export default function AuthPage({ mode }: { mode: "login" | "register" }) {
  const isLogin = mode === "login";
  return <main className="auth-page"><section className="auth-aside"><Link href="/" className="brand"><span><Sparkles size={15} /></span>Ferix<span>Builder</span></Link><div><p className="eyebrow"><ShieldCheck size={12} /> PRIVATE PROJECT WORKSPACE</p><h1>{isLogin ? <>PICK UP<br />WHERE YOU<br /><strong>LEFT OFF.</strong></> : <>START WITH<br />A CLEARER<br /><strong>FIRST STEP.</strong></>}</h1><p>{isLogin ? "Log in to return to your project workspace, previews, messages, and future delivery tools." : "Create your secure FerixBuilder account to save your project context and move through the delivery process with clarity."}</p></div><div className="auth-benefits"><span><CheckCircle2 /> Secure account access</span><span><CheckCircle2 /> Private project records</span><span><CheckCircle2 /> One place for previews and delivery</span></div></section><section className="auth-card"><Link href="/" className="back-home">← Back to home</Link><div><p className="eyebrow light">{isLogin ? "WELCOME BACK" : "CREATE YOUR ACCOUNT"}</p><h2>{isLogin ? "Log in to FerixBuilder" : "Create your FerixBuilder account"}</h2><p>{isLogin ? "Use the secure sign-in service connected to your FerixBuilder workspace." : "Registration is secure. If you already have an account, you can use the same sign-in service."}</p><button onClick={startAuthentication} className="auth-button">{isLogin ? "Log in securely" : "Create account securely"} <ArrowRight size={17} /></button><div className="auth-switch">{isLogin ? <>New to FerixBuilder? <Link href="/register">Create an account</Link></> : <>Already have an account? <Link href="/login">Log in</Link></>}</div><small>By continuing, you use FerixBuilder’s secure account service. Your dashboard will become available after the next product phase.</small></div></section></main>;
}

import { ArrowRight, CheckCircle2, MailCheck, ShieldCheck, Sparkles } from "lucide-react";
import { useState } from "react";
import { Link, useLocation } from "wouter";
import { getPreviewAccount, registerPreviewAccount, signInPreviewAccount, verifyPreviewAccount } from "@/lib/customerAccess";

export default function AuthPage({ mode }: { mode: "login" | "register" | "verify" | "forgot" | "reset" }) {
  const isLogin = mode === "login";
  const isRegister = mode === "register";
  const isVerify = mode === "verify";
  const isForgot = mode === "forgot";
  const [, setLocation] = useLocation();
  const [fullName, setFullName] = useState(""); const [email, setEmail] = useState(""); const [phone, setPhone] = useState(""); const [password, setPassword] = useState(""); const [confirmPassword, setConfirmPassword] = useState(""); const [acceptedTerms, setAcceptedTerms] = useState(false); const [code, setCode] = useState(""); const [feedback, setFeedback] = useState(""); const account = getPreviewAccount();
  const heading = isLogin ? "PICK UP WHERE YOU LEFT OFF." : isVerify ? "VERIFY YOUR ACCOUNT." : isForgot || mode === "reset" ? "GET BACK INTO YOUR ACCOUNT." : "START WITH A CLEARER FIRST STEP.";
  const title = isLogin ? "Welcome back" : isVerify ? "Check your email" : isForgot ? "Forgot your password?" : mode === "reset" ? "Create a new password" : "Create your account";
  const intro = isLogin ? "Sign in to continue with the project you have already set up." : isVerify ? `We sent a verification code to ${account?.email ?? "your email address"}.` : isForgot ? "Enter your email and we will prepare a reset route in this preview." : mode === "reset" ? "Choose a new password for your preview account." : "Create your account first. Business and project details come next in a guided setup.";
  const passwordIsValid = password.length >= 8 && /[a-z]/.test(password) && /[A-Z]/.test(password) && /\d/.test(password) && /[^A-Za-z0-9]/.test(password);
  const submit = () => {
    setFeedback("");
    if (isRegister) { if (!fullName.trim() || !email.trim() || !phone.trim() || !passwordIsValid || password !== confirmPassword || !acceptedTerms) return setFeedback("Complete every field, use a strong password, and accept the terms."); registerPreviewAccount({ fullName, email, phone }); setLocation("/verify-email"); return; }
    if (isLogin) { const signedIn = signInPreviewAccount(); if (!signedIn) return setFeedback("No verified preview account was found. Create and verify an account first."); setLocation(signedIn.onboardingComplete ? "/dashboard" : "/workspace/onboarding"); return; }
    if (isVerify) { if (code.replace(/\D/g, "").length !== 6) return setFeedback("Enter the six-digit verification code."); verifyPreviewAccount(); setLocation("/workspace/onboarding"); return; }
    if (isForgot) { if (!email.trim()) return setFeedback("Enter the email address connected to your account."); setFeedback("Preview reset link prepared. Continue to create a new password."); return; }
    if (!passwordIsValid || password !== confirmPassword) return setFeedback("Use matching strong passwords: 8+ characters with uppercase, lowercase, number, and special character."); setFeedback("Password updated in preview. You can now sign in.");
  };
  return <main className="auth-page"><section className="auth-aside"><Link href="/" className="brand"><span><Sparkles size={15} /></span>Ferix<span>Builder</span></Link><div><p className="eyebrow"><ShieldCheck size={12} /> CUSTOMER PROJECT WORKSPACE</p><h1>{heading.split(" ").map((word, index) => <span key={`${word}-${index}`}>{index === heading.split(" ").length - 1 ? <strong>{word}</strong> : word} {index === 1 && <br />}</span>)}</h1><p>{isLogin ? "A returning customer signs in and continues directly to the dashboard once onboarding is complete." : "Registration remains quick. The project brief is collected only after the account has been verified."}</p></div><div className="auth-benefits"><span><CheckCircle2 /> Separate account and project setup</span><span><CheckCircle2 /> Email verification before onboarding</span><span><CheckCircle2 /> Returning customers go to their dashboard</span></div></section><section className="auth-card"><Link href="/" className="back-home">← Back to home</Link><div><p className="eyebrow light">{isLogin ? "WELCOME BACK" : isVerify ? "VERIFY EMAIL" : isForgot || mode === "reset" ? "ACCOUNT RECOVERY" : "CREATE YOUR ACCOUNT"}</p><h2>{title}</h2><p>{intro}</p>{isVerify && <p className="auth-inline-note"><MailCheck size={17} /> For this protected preview, enter any six digits to simulate a verified email.</p>}<div className="auth-form">{isRegister && <><label>Full name<input value={fullName} onChange={(event) => setFullName(event.target.value)} placeholder="Your name" /></label><label>Email address<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" /></label><label>WhatsApp or phone number<input value={phone} onChange={(event) => setPhone(event.target.value)} placeholder="+234 000 000 0000" /></label></>}{(isLogin || isForgot) && <label>Email address<input type="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder="you@example.com" /></label>}{isVerify && <label>Verification code<input className="verify-code" inputMode="numeric" value={code} onChange={(event) => setCode(event.target.value.replace(/\D/g, "").slice(0, 6))} placeholder="000000" /></label>}{(isLogin || isRegister || mode === "reset") && <><label>{mode === "reset" ? "New password" : "Password"}<input type="password" value={password} onChange={(event) => setPassword(event.target.value)} placeholder="At least 8 characters" /></label>{(isRegister || mode === "reset") && <label>Confirm password<input type="password" value={confirmPassword} onChange={(event) => setConfirmPassword(event.target.value)} placeholder="Repeat your password" /></label>}</>}{(isRegister || mode === "reset") && <p className="password-rules">Password must include 8+ characters, uppercase, lowercase, a number, and a special character.</p>}{isRegister && <label className="auth-check"><input type="checkbox" checked={acceptedTerms} onChange={(event) => setAcceptedTerms(event.target.checked)} />I agree to the Terms of Service and Privacy Policy.</label>}{isLogin && <div className="auth-links"><label className="auth-check"><input type="checkbox" />Remember me</label><Link href="/forgot-password">Forgot password?</Link></div>}{feedback && <p className={feedback.startsWith("Preview") || feedback.startsWith("Password updated") ? "auth-success" : "auth-error"}>{feedback}</p>}<button onClick={submit} className="auth-button">{isLogin ? "Sign in" : isRegister ? "Create account" : isVerify ? "Verify and continue" : isForgot ? "Send reset link" : "Reset password"} <ArrowRight size={17} /></button>{isForgot && feedback.startsWith("Preview") && <Link href="/reset-password" className="auth-button">Create new password <ArrowRight size={17} /></Link>}</div><div className="auth-switch">{isLogin ? <>New to FerixBuilder? <Link href="/register">Create an account</Link></> : isRegister ? <>Already have an account? <Link href="/login">Sign in</Link></> : isForgot ? <>Remembered it? <Link href="/login">Return to sign in</Link></> : mode === "reset" ? <Link href="/login">Continue to login</Link> : <>Wrong email? <Link href="/register">Create another account</Link></>}</div><small>This is a client-side preview of the journey. Production account credentials, verification, and password recovery must use the existing secure identity service.</small></div></section></main>;
}

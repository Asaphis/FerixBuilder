import { ArrowRight, CheckCircle2, ShieldCheck, Sparkles } from "lucide-react";
import { Link, useLocation } from "wouter";

export default function AuthPage({ mode }: { mode: "login" | "register" }) {
  const isLogin = mode === "login";
  const [, setLocation] = useLocation();
  return <main className="auth-page"><section className="auth-aside"><Link href="/" className="brand"><span><Sparkles size={15} /></span>Ferix<span>Builder</span></Link><div><p className="eyebrow"><ShieldCheck size={12} /> PRIVATE PROJECT WORKSPACE</p><h1>{isLogin ? <>PICK UP<br />WHERE YOU<br /><strong>LEFT OFF.</strong></> : <>START WITH<br />A CLEARER<br /><strong>FIRST STEP.</strong></>}</h1><p>{isLogin ? "Enter the preview workspace to explore your project status, previews, messages, and future delivery tools." : "Create a preview workspace to explore the customer journey before the real account system is enabled."}</p></div><div className="auth-benefits"><span><CheckCircle2 /> Preview-mode access</span><span><CheckCircle2 /> Project workspace demonstration</span><span><CheckCircle2 /> No account setup required</span></div></section><section className="auth-card"><Link href="/" className="back-home">← Back to home</Link><div><p className="eyebrow light">{isLogin ? "WELCOME BACK" : "CREATE YOUR ACCOUNT"}</p><h2>{isLogin ? "Log in to FerixBuilder" : "Create your FerixBuilder account"}</h2><p>{isLogin ? "During this preview, sign-in opens the dashboard directly—nothing is locked." : "During this preview, account creation opens the dashboard directly—nothing is locked."}</p><button onClick={() => setLocation("/dashboard")} className="auth-button">{isLogin ? "Enter preview dashboard" : "Create preview workspace"} <ArrowRight size={17} /></button><div className="auth-switch">{isLogin ? <>New to FerixBuilder? <Link href="/register">Create a preview account</Link></> : <>Already have an account? <Link href="/login">Log in to preview</Link></>}</div><small>Preview mode is active. Secure production authentication will be restored when the full account experience is ready.</small></div></section></main>;
}
